{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE" },
  "deploy": {
    "startCommand": "bash -lc \"python -m uvicorn main_darbottarolo:app --host 0.0.0.0 --port ${PORT:-8080} --workers 1 --log-level debug\""
  }
}
